#include "main.h"
/**
 * add - a function that adds j integers and returns the result
 * @i: first number input
 * @j: second number input
 * Return: i + j
 */
int add(int i, int j)
{
	return (i + j);
}
